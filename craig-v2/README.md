Craig watcher demo stub

This folder provides a minimal Node.js service so the project's `docker-compose.yml` can build successfully.

Replace this with the real `craig-v2` service implementation when available.
