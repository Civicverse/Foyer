Civicverse demo (minimal)

This is a small runnable demo used for local development. Build with:

npm ci
npm run build

Then serve the `dist` directory (nginx Dockerfile is configured to build this app).